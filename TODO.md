# Portfolio split & local host — TODO

- [ ] Create project files from `code.md` into correct paths (Vite + React + Tailwind + Framer Motion)
- [ ] Ensure `src/components/*`, `src/components/common/*`, `src/hooks/*`, `src/data/*` exist with the correct contents
- [ ] Run `npm install`
- [ ] Run `npm run dev` and verify the portfolio loads locally
- [ ] (If needed) Fix any build/runtime errors due to missing files/import paths
